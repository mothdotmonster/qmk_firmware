# the Programmer's Numpad

note: if building for QT Py, replace matrix_pins block with the following:

```
"rows": ["GP29", "GP28", "GP27", "GP26", "GP24", "GP25", "GP20"],
"cols": ["GP3", "GP4", "GP6", "GP5"]
```